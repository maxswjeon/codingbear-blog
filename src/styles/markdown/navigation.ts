import {createGlobalStyle} from "styled-components";

const PageNavigationStyle = createGlobalStyle`
  .left {
    text-align: left;
  }
  .right {
    text-align: right;
  }
  .center {
    text-align: center;
  }
`;

export default PageNavigationStyle;
